name: petrol_price_za
description: "Find cheapest petrol and diesel in South Africa"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Core packages
  google_maps_flutter: ^2.14.2
  google_maps_webservice2: ^1.0.5
  geolocator: ^14.0.2
  url_launcher: ^6.3.2
  http: ^1.2.2 # ← Must be ^1.2.2 or higher

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
